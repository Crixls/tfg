{% extends 'base.html.twig' %}

{% block title %}OrderEntity{% endblock %}

{% block body %}
    <h1>OrderEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_entity.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ order_entity.date ? order_entity.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ order_entity.state }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ order_entity.total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_order_entity_index') }}">back to list</a>

    <a href="{{ path('app_order_entity_edit', {'id': order_entity.id}) }}">edit</a>

    {{ include('order_entity/_delete_form.html.twig') }}
{% endblock %}
