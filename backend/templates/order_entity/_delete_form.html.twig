<form method="post" action="{{ path('app_order_entity_delete', {'id': order_entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order_entity.id) }}">
    <button class="btn">Delete</button>
</form>
